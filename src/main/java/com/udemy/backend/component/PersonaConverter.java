package com.udemy.backend.component;


//@Component("personaConverter")
public class PersonaConverter {
	/*
	@Autowired
	@Qualifier("ConsultoresServiceImpl")
	private Consultores consultoresService;
	
	@Autowired
	@Qualifier("consultoresEmpresaKabecServiceImpl")
	private ConsultoresEmpresaKabecService consultoresEmpresaKabecService;
	
	private Integer id;
	
	

	public Personas converterPersonaModel2Persona(PersonaModel personaModel){
		Personas persona=new Personas();
		persona.setNombre(personaModel.getNombre());
		persona.setApellidoPaterno(personaModel.getApellidoPaterno());
		persona.setApellidoMaterno(personaModel.getApellidoMaterno());
		persona.setFechaNacimiento(personaModel.getFechaNacimiento());
		persona.setRfc(personaModel.getRfc());
		persona.setCurp(personaModel.getCurp());
		persona.setCorreo(personaModel.getCorreo());
		persona.setTelparticular(personaModel.getTelparticular());
		persona.setTelCelular(personaModel.getTelCelular());
		persona.setEdad(personaModel.getEdad());
		persona.setIdpersona(personaModel.getIdpersona());
		//persona.setEstadocivil(consultoresService.findByIdEstadoC(id));
		//persona.setGenero(consultoresService.findByIdGenero(id));
		return persona;
	}
	
	public PersonaModel convertPersona2PersonaModel(Personas persona){
		PersonaModel personaModel=new PersonaModel();
		personaModel.setNombre(persona.getNombre());
	    personaModel.setApellidoPaterno(personaModel.getApellidoPaterno());
	    personaModel.setApellidoMaterno(persona.getApellidoMaterno());
	    personaModel.setFechaNacimiento(persona.getFechaNacimiento());
	    personaModel.setRfc(persona.getRfc());
	    personaModel.setCurp(personaModel.getCurp());
	    personaModel.setCorreo(persona.getCorreo());
	    personaModel.setTelparticular(persona.getTelparticular());
	    personaModel.setTelCelular(persona.getTelCelular());
        personaModel.setEdad(persona.getEdad());
        personaModel.setIdpersona(persona.getIdpersona());
        //personaModel.setEstadoCivil(consultoresService.findByIdEstadoC(id).toString());
        //personaModel.setGenero(consultoresService.findByIdGenero(id).toString());      
		return personaModel;	
	}
	
	*/
}
